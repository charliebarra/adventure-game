# Upload instructions

1. Create a new public GitHub repository named `adventure-game`.
2. Upload the **contents** of this folder so `README.md`, `src`, and `images` appear at the repository root.
3. Commit the files. GitHub will display the case study automatically from `README.md`.

The original source file is already inside `src/` and should not be edited during upload.
